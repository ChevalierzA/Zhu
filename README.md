# 贪吃蛇小游戏

这是一个基于 `curses` 的简单终端版贪吃蛇游戏示例。

## 运行方法

确保系统安装有 Python 3，并在支持 `curses` 的终端环境中运行：

```bash
python3 snake_game.py
```

游戏通过方向键控制蛇移动，碰到边界或自身时游戏结束。

## 老虎机小游戏

同样确保系统安装有 Python 3，直接运行：

```bash
python3 slot_machine.py
```

按提示按下 Enter 即可拉杆，输入 `q` 可退出游戏。
